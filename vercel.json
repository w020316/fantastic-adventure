{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db seed && next build",
  "installCommand": "npm install",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ':(exclude)docs/**'"
}
